<svg:svg [attr.x]="node.x" [attr.y]="node.y">
    <svg:rect draggable="true" width="100" height="100" fill="red" rx="5" ry="5"></svg:rect>
    <svg:text x="0" y="20">({{node.x}},{{node.y}}) {{node.name}}</svg:text>
</svg:svg>