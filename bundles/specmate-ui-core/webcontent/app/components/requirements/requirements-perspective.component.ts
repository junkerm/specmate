import { Component, OnInit } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: "requirements-perspective",
    templateUrl: "requirements-perspective.component.html"
})

export class RequirementsPerspective implements OnInit {
    ngOnInit(): void {
        
    }
}