<svg:g>
    <svg:rect [attr.x]="x" [attr.y]="y" width="100" height="100" fill="red" rx="5" ry="5"></svg:rect>
    <svg:text x="0" y="50" font-family="Verdana" font-size="35" fill="blue">{{node.name}}</svg:text>
</svg:g>